#!/bin/sh
set -eu

if [ "${1:-}" = info ]; then
  printf '{"format":"%s"}\n' "${QEMU_IMG_FORMAT:-qcow2}"
  exit 0
fi

printf '%s\n' "$*" >> "$QEMU_IMG_LOG"
output=
for argument
do
  output=$argument
done
printf '%s\n' "overlay" > "$output"
